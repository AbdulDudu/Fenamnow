pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun run format
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun run lint

pre-push:
  commands:
    testing-build:
      run: bun run build
