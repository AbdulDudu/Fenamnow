import { Card, CardHeader, CardTitle } from "@fenamnow/ui/components/ui/card";

export default function NotificationsSettings() {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle>Notifications</CardTitle>
      </CardHeader>
    </Card>
  );
}
